(ns clojureslim.statement-executor-spec
  (:use [speclj.core]
        [clojureslim.statement-executor])
  )

(describe "statement-executor"
  (it "should be"
    (should (make-statement-executor)))
          )
